apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: omnia-api
spec:
  selector:
    app: omnia-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP